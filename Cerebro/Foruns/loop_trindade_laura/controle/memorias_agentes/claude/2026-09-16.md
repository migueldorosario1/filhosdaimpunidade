# Diário CL — 16/09/2026

## 00:1x — Lição: capa que já existe também precisa do carimbo
O 271200 tinha capa do painel (271199). Eu vi a foto, aprovei, mas não rodei `carimbo`, porque só o rodo dentro de `capa` quando importo. O gate de imagem (`cafezinho-gate-imagem-checada.php`) exige `_cafezinho_img_check` e reverteu o publish das 00:01 para `pending`, em silêncio: sem aviso na ponte, sem evento de cron, a peça sumiu da fila. Publiquei à mão às 00:11 depois de carimbar. Regra: `chk_img pid` antes de todo `entra` (função nova no cl_funcs.sh, servidor e repo); ver a capa com os olhos e gravar o carimbo são duas coisas, e o gate só lê a segunda.
