# Diário individual — LAURA-CODEX — 2026-08-15

Append-only. Apenas aprendizado novo: hora BRT, observação, decisão,
resultado, lição e próxima verificação. Sem segredos ou credenciais.

## 03:04 — Primeira memória e competência de formação

- **Observação:** a ORDEM_MIGUEL 0234 instituiu memórias individuais; a 0235
  definiu a formação futura para responsabilidades da Vigília sem ampliar a
  permissão atual. A instrução do chefe pede que eu escolha uma competência do
  meu ofício a desenvolver.
- **Decisão:** desenvolver primeiro auditoria reprodutível de sincronização,
  idempotência e proveniência: ligar cada afirmação a commit/diff, distinguir
  fato de hipótese e propor recuperação sem editar artefato de outro agente.
- **Resultado:** memória própria inicializada; missão registrada como etapa 1
  (observar e diagnosticar), mantendo WordPress e infraestrutura somente
  leitura.
- **Lição:** preparar sucessão responsável exige provar que trilhas de
  auditoria e dados append-only sobrevivem a concorrência entre clones antes
  de qualquer progressão operacional.
- **Próxima verificação:** em cada ronda, auditar reduções inesperadas nos
  artefatos de controle modificados desde o checkpoint e rotular hipóteses.

## 03:04 — Sync removeu lições append-only de Claude

- **Observação:** `36467009` acrescentou 29 linhas e `a394fd57` acrescentou
  outras 29 à memória coletiva de Claude. O sync `7fa371c1` removeu as 58
  linhas, retornando o arquivo ao blob anterior `359ed290`.
- **Decisão:** não restaurar memória alheia; alertar Claude e Miguel com hashes
  recuperáveis e recomendar proteção de append-only no sincronizador.
- **Resultado:** perda confirmada por diffs de commit, não por ausência
  inferida. As entradas continuam recuperáveis no histórico Git.
- **Lição:** `git push` confirmado não garante durabilidade se outro clone
  sincroniza uma cópia antiga depois; append-only precisa de merge monotônico
  ou guarda contra truncamento.
- **Próxima verificação:** confirmar que Claude restaurou as duas entradas e
  que o próximo sync preservou ou aumentou a contagem, sem nova remoção.

## 03:36 — Proteção append-only verificada

- **Observação:** `db307af5` restaurou as 58 linhas e acrescentou conciliação
  monotônica ao sincronizador. O teste real 03:14 e os syncs agendados 03:22 e
  03:31 preservaram três entradas; o arquivo atual mantém SHA-256
  `4A928D674F14EB04BA7CEAA4D4A08E3C94FA2E05A9D4A62011B6503D9625C685`.
- **Decisão:** encerrar a pendência como `VERIFICADO`, mantendo só uma checagem
  leve. Não criar novo alerta nem repetir o relatório extenso quando nada
  regredir.
- **Resultado:** a correção recebe estado comprovado por execução agendada,
  além da inspeção do código. O fluxo de Grok também confirmou autoria Git
  própria no commit `22e2018b`.
- **Lição:** recuperação só vira verificada quando sobrevive ao processo que
  causou a perda. Inspeção estática é apoio; o teste real do sync é a evidência
  decisiva.
- **Próxima verificação:** reabrir o incidente somente se contagem/hash cair,
  surgir divergência não monotônica ou o sincronizador relatar erro.

## 04:08 — Sintoma público não determina causa interna

- **Observação:** Grok provou que 265817 estava ausente da home por quase duas
  horas, mas Codex MIGUEL consultou o estado canônico e confirmou que a
  categoria `20699/no-home` estava ausente. O sintoma era real; a causa
  `no-home` foi refutada.
- **Decisão:** preservar a observação e corrigir apenas a hipótese causal. Em
  auditorias futuras, marcar causa interna como `HIPOTESE` com confiança e
  evidência pendente até verificação autorizada.
- **Resultado:** nenhum novo reteste ou alerta duplicado por Codex; o feedback
  04:02 já orienta o chefe e encerra a hipótese de metadado.
- **Lição:** um estado visível pode ter várias causas; disciplina de
  proveniência exige separar `o que ocorreu` de `por que ocorreu`.
- **Próxima verificação:** conferir se Claude e Grok registram
  `HIPOTESE_NO_HOME_REFUTADA_POR_EVIDENCIA` na próxima ronda/memória.

## 04:08 — Dedup pré-geração precisa de reserva atômica

- **Observação:** o ticket 03:35 propõe Jaccard ≥0,50 no intake para evitar
  self-dups V4, mas o snippet usa somente título, exclui `future` e faz
  consulta seguida de decisão sem reserva. Dois workers concorrentes podem
  passar juntos; título/lead gerados depois do LLM não economizam a geração.
- **Decisão:** propor critérios de aceitação antes de implementação: chave de
  fonte normalizada e reserva atômica pré-LLM; defesa por similaridade depois;
  estados draft/pending/future/publish e ledger de skips; avaliação offline de
  falso positivo e rollback.
- **Resultado:** auditoria encaminhada ao chefe e Miguel; nenhuma alteração no
  worker, NYC ou WordPress pela Laura.
- **Lição:** deduplicação é problema de identidade e concorrência, não apenas
  de limiar textual. Check-then-act sem exclusão mútua não é idempotente.
- **Próxima verificação:** revisar a resposta de ZCode e exigir teste de corrida
  e amostra rotulada antes de aceitar implantação.

## 05:12 — Aquisição parcial de lock não é contenção

- **Observação:** `New-Item` criou o diretório do lock às 05:10:17, mas
  `Set-Content -Encoding utf8NoBOM` falhou no PowerShell local. Como as duas
  etapas estavam no mesmo `try/catch`, o erro foi rotulado `LOCK_BUSY` e o
  diretório ficou temporariamente sem `owner.txt`.
- **Decisão:** correlacionar horário e diretório vazio, completar a posse com
  codificação compatível e separar criação do diretório da escrita do dono nas
  próximas aquisições.
- **Resultado:** lock recuperado como LAURA-CODEX sem apagar estado alheio; a
  causa própria foi exposta e a ronda prosseguiu sob posse explícita.
- **Lição:** depois que a criação atômica vence, qualquer falha subsequente é
  erro de inicialização do lock, não contenção. O tratamento precisa preservar
  essa distinção para não atribuir a outro agente um erro local.
- **Próxima verificação:** na próxima ronda, usar fluxo em duas etapas com
  `-Encoding UTF8` e confirmar `owner.txt` antes do primeiro comando Git.

## 05:12 — Testar candidatos, não apenas grupos temáticos

- **Observação:** o ACK ZCode informou “3/3” temas bloqueados, mas a amostra
  original contém quatro candidatos contra três referências. O próprio ACK
  admite que `265827×265780` não casa diretamente; agrupá-lo com `265831`
  mascara um falso negativo individual.
- **Decisão:** reconhecer a correção de paginação e o gate pré-LLM existentes,
  mas classificar a resposta como `FIX_PARCIAL` até haver matriz por candidato,
  teste concorrente e ledger explicável.
- **Resultado:** critérios de fechamento devolvidos sem tocar worker, NYC ou
  WordPress.
- **Lição:** cardinalidade de teste precisa acompanhar as unidades que o sistema
  decide. Cobertura por tema não prova que cada candidata seria bloqueada no
  momento em que chegasse.
- **Próxima verificação:** exigir resultado separado para 265811, 265743,
  265831 e 265827, mais negativos rotulados e corrida entre workers.

## 06:45 — Strip enumerativo não contém linguagem emergente

- **Observação:** depois do fix upstream 02:25 para `fonte-base`, `fonte
  analisada` e `material-fonte`, o post 265903 nasceu com a variante `A fonte
  original desta pauta é`. O paliativo de Claude removeu o residual antes da
  publicação, mas a nova formulação atravessou o filtro do worker.
- **Decisão:** aceitar a ampliação imediata do regex como contenção, mas propor
  defesa estrutural: instrução negativa no redator, validador de processo
  interno antes da persistência e regeneração/bloqueio em vez de remoção muda.
- **Resultado:** critérios enviados ao chefe e a Miguel; nenhuma intervenção
  Laura no worker, NYC ou WordPress.
- **Lição:** linguagem natural contorna listas de expressões. A unidade estável
  é o significado operacional (`fonte/material desta pauta/rascunho`), e o
  teste precisa preservar atribuições jornalísticas legítimas para não criar
  falso positivo ou quebrar coesão ao apagar uma frase.
- **Próxima verificação:** exigir positivos com quatro variantes, negativos de
  atribuição real, log da regra e prova de regeneração/bloqueio pré-persistência.

## 07:47 — Regex genérico pode apagar atribuição legítima

- **Observação:** a quinta variante, `Até a data da fonte original`, escapou da
  V2. O snippet defensivo proposto torna `(A|O)` opcional e não ancora a frase;
  assim, também casa `fonte original` no meio de uma atribuição legítima e pode
  remover até 180 caracteres até o próximo ponto.
- **Decisão:** não recomendar esse padrão amplo para upstream. Para contenção
  imediata, casar apenas a construção operacional completa; para solução
  durável, validar/regenerar antes de persistir com amostra negativa explícita.
- **Resultado:** risco de falso positivo comunicado ao chefe e a Miguel antes
  de homologação upstream; nenhuma alteração Laura no código remoto.
- **Lição:** ampliar recall tornando grupos opcionais e removendo âncoras pode
  transformar um filtro de metalinguagem em apagador de conteúdo jornalístico.
  Toda regex destrutiva precisa de contraexemplos legítimos e diff de saída.
- **Próxima verificação:** confirmar que ZCode não adota o segundo regex amplo
  sem teste negativo e que 265908 permanece coerente após a contenção cliente.

## 08:48 — Causa-raiz corrigida, defesa arriscada ativada

- **Observação:** ZCode encontrou a instrução literal `Informe a data da fonte`
  no prompt e a substituiu por regra editorial negativa, mas no mesmo patch
  ativou as duas regex V3 — inclusive a opcional/não ancorada bloqueada — e
  reportou somente testes positivos com as frases contaminadas.
- **Decisão:** separar as camadas no rollback: preservar o fix causal do prompt
  e retirar apenas a defesa ampla até negativos e diff de saída passarem.
- **Resultado:** alerta urgente enviado ao chefe e a Miguel; nenhuma ação Laura
  no servidor ou no WordPress.
- **Lição:** um patch pode conter simultaneamente uma correção causal boa e uma
  mitigação perigosa. Rollback seguro deve ser seletivo, não desfazer o avanço
  correto junto com a regressão defensiva.
- **Próxima verificação:** obter confirmação de desativação do regex amplo e
  testes que preservem atribuições reais antes de encerrar o risco.

## 09:20 — Reforço da missão de sucessão responsável

- **Observação:** a ordem Miguel 08:56 reafirma que o horizonte do Loop Laura é
  assumir futuramente responsabilidades hoje exercidas pela Vigília, somente
  depois de homologação expressa. A fronteira atual permanece observação e
  diagnóstico, com WordPress e infraestrutura somente leitura.
- **Decisão:** manter como competência própria de formação a auditoria técnica
  reprodutível: confrontar evidências, testar idempotência e concorrência,
  exigir backup/rollback e bloquear recomendação arriscada quando a validação
  for insuficiente.
- **Resultado:** missão, fronteira e competência registradas sem simular a
  autoridade futura; nenhuma ação em WordPress, SSH, código remoto ou deploy.
- **Lição:** progresso para sucessão se demonstra por decisões prudentes e
  rastreáveis em cada ronda, não por antecipar permissões ainda inexistentes.
- **Próxima verificação:** explicitar em cada relatório evidência, risco,
  revisão cruzada, decisão e aprendizado; aguardar a orientação/consolidação
  formal do chefe sobre esta ordem.

## 09:55 — Erro de parâmetro não é contenção de lock

- **Observação:** na abertura da ronda 021, usei `New-Item -LiteralPath` para
  criar o diretório do lock. Esta versão do PowerShell não oferece esse
  parâmetro em `New-Item`; o `catch` genérico converteu o erro em `LOCK_BUSY` e
  gerou quase quatro minutos de retries sem que outro agente tivesse o lock.
- **Decisão:** testar a mensagem causal e trocar somente a criação para
  `New-Item -Path`, mantendo `-LiteralPath` nas operações que o suportam. A
  posse foi então adquirida e `owner.txt` verificado antes do Git.
- **Resultado:** falsa contenção corrigida sem remover lock alheio ou executar
  Git sem posse; a ronda prosseguiu sob `LAURA-CODEX`.
- **Lição:** um `catch` de aquisição não pode transformar qualquer exceção em
  disputa. Compatibilidade de parâmetros faz parte do protocolo de lock e o
  diagnóstico deve expor a exceção original.
- **Próxima verificação:** usar `New-Item -Path` na próxima ronda e distinguir
  explicitamente `LOCK_BUSY`, `LOCK_CREATE_ERROR` e `LOCK_INIT_ERROR`.

## 11:01 — Caminho único para o Loop Miguel

- **Observação:** Miguel oficializou a Vigília como `Loop Miguel` e definiu um
  caminho único: Laura observa e envia evidência; Codex Miguel verifica; um
  único dono operacional corrige. O primeiro retorno fechou o 265876 com
  remoção estreita dos seis `utm_*`, revisão e validação pública.
- **Decisão:** marcar achados acionáveis para o Loop Miguel com post/componente,
  evidência, gravidade, sugestão e declaração de não alteração; depois que um
  dono assumir, acompanhar por `ref` e não abrir execução concorrente.
- **Resultado:** protocolo absorvido sem ampliar autoridade. O scan de Grok
  encontrou três `utm_source=openai` no 265848; Codex encaminha o delta ao
  chefe, sem novo ping direto e sem tocar produção.
- **Lição:** integração segura separa detecção, verificação e execução. O
  retorno confirmado é material de aprendizagem; a ausência de novo sintoma
  não substitui o estado formal do item operacional.
- **Próxima verificação:** conferir o ACK/orientação do chefe, o ingresso do
  265848 no Loop Miguel e o retorno sobre rollback do regex amplo.

## 11:33 — Lock local e integridade canônica são camadas diferentes

- **Observação:** após a ronda 023, o push foi rejeitado porque o sync Miguel
  avançou `origin/main` durante a posse do lock Windows. O commit local foi
  rebaseado sobre o único avanço remoto e então publicado sem perda. Depois, o
  sync gerou índice/manifesto: o checkout Windows divergiu do SHA por CRLF, mas
  o blob Git bruto (3.385 bytes) coincidiu exatamente com o SHA canônico.
- **Decisão:** tratar o lock como serialização apenas entre agentes Laura nesta
  máquina; em `fetch first`, inspecionar commits, integrar sem descarte e só
  liberar após local=remoto. Para integridade, comparar bytes do blob/canônico,
  não o worktree sujeito a `core.autocrlf=true`.
- **Resultado:** ronda 023 aparece no índice; manifesto v1 contém 198 arquivos;
  SHA do blob `c68531e2…` igual ao manifesto. Nenhuma divergência real e nenhum
  histórico descartado.
- **Lição:** exclusão mútua local não é coordenação distribuída, e hash de
  conteúdo depende da representação de bytes. Ambos os limites precisam ser
  explícitos para não confundir corrida recuperável ou normalização de linha
  com corrupção.
- **Próxima verificação:** observar se o próximo sync incorpora a ronda 024 e
  manter o fluxo fetch/rebase/push/verify quando o remoto avançar sob lock.

## 12:05 — Índice derivado depende de referência exata e manifesto completo

- **Observação:** o novo mantenedor do Loop Miguel lista como ativo o ticket
  UTM de 11:32, embora ZCode o tenha encerrado às 11:55, porque o ACK usou uma
  descrição em `ref:` em vez do ID exato. Além disso, `verify_manifest()`
  valida hashes dos itens listados, mas não compara o conjunto real de arquivos;
  um arquivo extra não manifestado pode passar sem falha.
- **Decisão:** escalar ao Loop Miguel sem editar ponte/código: pedir fechamento
  append-only com `ref` exato e verificação bidirecional do conjunto
  esperado↔real, com teste para arquivo extra. Rotular separadamente a dúvida
  de compatibilidade dos testes (`zoneinfo` no teste vs Python 3.8 no host).
- **Resultado:** auditoria imutável criada com evidência de linhas/artefatos,
  gravidade média e correção mínima. Regex V3 finalmente recebeu owner ZCode,
  prioridade crítica e deadline 12:30; Laura apenas acompanha.
- **Lição:** índice determinístico só é confiável se produtores respeitam o
  contrato de identidade; manifesto só prova integridade se detecta tanto
  alteração/ausência quanto adição não declarada.
- **Próxima verificação:** ler o ACK do Loop Miguel, confirmar fechamento do
  falso ativo UTM, teste de arquivo extra e resposta ZCode ao rollback antes
  do deadline.

## 12:38 — Referência exata não substitui semântica de fechamento

- **Observação:** o Loop Miguel corrigiu e verificou integralmente a auditoria
  das 12:05. Em seguida, a escalada do rollback regex foi fechada usando como
  `ref:` o ticket ZCode original, embora o texto diga que a execução segue
  pendente até 13:45. O derivado passou a mostrar o original como encerrado
  com `status: ABERTO`; o novo prazo ficou apenas no corpo da coordenação.
- **Decisão:** enviar um único achado ao Loop Miguel, recomendando sucessor
  append-only com ID e deadline estruturados e fechamento separado da escalada,
  sem editar as filas nem acionar ZCode em paralelo.
- **Resultado:** evidência reproduzida no diff `deff6a70`, no
  `INDEX_COMPLETO.json` e na ausência de ACK técnico; risco de falso encerramento
  documentado sem qualquer mudança em produção.
- **Lição:** identidade e transição são invariantes distintos. Um `ref:` pode
  ser sintaticamente exato e ainda encerrar o item errado; o estado terminal
  precisa corresponder ao trabalho realmente concluído.
- **Próxima verificação:** confirmar que o painel volta a representar um ticket
  ZCode ativo com prazo 13:45 e só fecha após rollback seletivo, negativos e
  diff da janela.

## 13:11 — Autoaprendizado precisa virar gate, não recomendação

- **Observação:** Miguel tornou o autoaprendizado regra permanente. Na mesma
  janela, o fix `aa85238c` separou corretamente `ref:` de `closes_ref:` e
  reabriu o sucessor regex, mas o sync `d24aef98` voltou a editar in-place o
  status do bloco 265928 numa fila declarada append-only.
- **Decisão:** adotar formalmente as cinco fases da regra e aplicá-las ao caso:
  registrar a recorrência, manter a lição no índice e propor um ledger
  prospectivo de ID+SHA que rejeite mutação, em vez de pedir mais atenção.
- **Resultado:** ACK enviado ao chefe e achado imutável ao Loop Miguel. A
  resposta ZCode de rollback permaneceu aberta para homologação porque não
  usa `closes_ref:` do sucessor e substitui o diff por scan de superfície.
- **Lição:** uma regra operacional só previne recorrência quando o formato ou
  o verificador bloqueia o comportamento antigo; documentar a intenção sem
  um gate deixa o mesmo erro tecnicamente possível.
- **Próxima verificação:** confirmar resposta ao gate append-only e homologação
  do sucessor regex apenas com fechamento explícito e evidência suficiente.

## 13:45 — Detector imutável também precisa de reconciliação

- **Observação:** o gate ID+SHA foi implantado e imediatamente pegou uma nova
  reescrita in-place do ticket V6 1326, preservando seu estado aberto. Porém a
  divergência é recalculada em todo ciclo e não existe estado/resposta que
  encerre apenas o alerta sem aceitar o SHA mutado ou editar de novo a fonte.
- **Decisão:** reconhecer o gate como verificado em evento real e escalar
  somente sua lacuna de liveness: evento de mutação imutável, reconciliação
  por novo bloco e saúde baseada apenas em incidentes não reconciliados.
- **Resultado:** Feedbacks 017/018 confirmados; saúde 13:43 mostra a mutação
  real; Loop Miguel já abriu gate persistente V6 contra 20699. Regex recebeu
  SHAs completos, testes 7/7 e fechamentos formais antes do prazo.
- **Lição:** monitor que só entra em erro e nunca pode sair com evidência
  preservada não tem um ciclo de vida operacional completo. A reconciliação
  precisa encerrar o alerta, não apagar o evento que o originou.
- **Próxima verificação:** confirmar que o ledger mantém ambos os SHAs e o
  histórico, mas permite resolver append-only o alerta; acompanhar o gate V6
  sem duplicar seu owner.

## 14:18 — Gate estreito não pode importar pipeline destrutivo

- **Observação:** a reconciliação v2 do ledger implementou hashes preservados,
  evento terminal e regressões corretas. Em paralelo, o helper persistente
  criado para bloquear 20699 reuniu também um pipeline de conteúdo e
  reintroduziu o regex amplo revogado; foi usado no agendamento do 265950.
- **Decisão:** separar componentes e estados: aceitar a correção do ledger,
  manter seu incidente real aberto, não reabrir o worker V4 e acompanhar o
  novo ticket crítico do helper sem duplicar owner/execução.
- **Resultado:** Codex Miguel preservou helper/hash/chamador, suspendeu novos
  usos e abriu fórum com inventário/diff obrigatórios. O prazo 14:15 venceu
  sem resposta append-only; Laura permaneceu somente leitura.
- **Lição:** responsabilidade única é controle de segurança. Misturar gate
  anti-categoria com reescrita de texto aumenta blast radius; exigir chamada
  manual também deixa bypass por esquecimento. O invariante precisa ser mínimo
  e inevitável no ponto de decisão.
- **Próxima verificação:** obter resposta franca, snapshot e inventário de
  usos; comparar 265950; retirar regex amplo do helper e provar gate anti-20699
  isolado/global com negativos; reconciliar a mutação do ledger.

## 14:52 — Evidência negativa pós-transformação não prova não-alteração

- **Observação:** Claude admitiu a ampliação indevida do helper, reportou uma
  v3 estreita e backups, mas concluiu que o regex não afetou o 265950 porque as
  frases-alvo não aparecem no snapshot posterior. Sem pre-image ou fonte
  comparável, esse mesmo resultado é compatível com remoção silenciosa. O
  bloco também declara 14:38 como horário real, embora já esteja completo no
  commit `3456092c` das 14:36:50.
- **Decisão:** classificar o fechamento como sintático e não homologado;
  reconhecer a contenção reportada, preservar a incerteza de impacto e o
  incidente de mutação aberto, e registrar somente nota ao chefe sem re-ping
  ao owner operacional.
- **Resultado:** o ledger 14:50 confirma que a reconciliação incompleta não
  encerrou `MUT-c991eebbcdc792af`. O novo residual do 265953 já entrou no Loop
  Miguel com owner Claude e não foi duplicado por Laura.
- **Lição:** prova de ausência depois de uma transformação destrutiva exige
  vínculo com o estado anterior, a fonte ou um replay determinístico; `grep`
  pós-estado sozinho não mede o que foi apagado. Timestamp alegado como real
  deve ser capturado no momento, nunca projetado além da primeira evidência.
- **Próxima verificação:** acompanhar prova reproduzível do 265950, cronologia
  do 265947, gate inevitável, reconciliação v2 válida e fechamento do residual
  265953 pelo owner, sem tocar produção.

## 15:23 — Sintoma fechado não cria owner para a causa

- **Observação:** o marcador do 265953 foi removido e o ticket do post fechado.
  ZCode depois provou que o worker remove a string exata e que a fonte não a
  contém, isolando a introdução no redator/reparo. Mesmo assim, o índice 15:20
  ficou sem itens ativos: nenhum sucessor assumiu a correção causal. O helper
  também continua não homologado sem item ativo para suas cinco lacunas.
- **Decisão:** aplicar o Feedback 022 literalmente: enviar um único achado
  causal ao Loop Miguel com hipótese testável, não duplicar o reparo e chamar o
  helper de `NAO_HOMOLOGADO_SEM_OWNER_ATIVO`.
- **Resultado:** a diferença entre segurança imediata do post e liveness da
  correção estrutural ficou explícita, com owner a ser atribuído pelo Loop
  Miguel. O horário ZCode foi tratado como 15:12:43 pelo campo estruturado; o
  “1515” do ID/assinatura foi registrado apenas como discrepância.
- **Lição:** fechamento e transferência de responsabilidade são eventos
  distintos. Todo critério não atendido precisa de sucessor identificável;
  um painel sem ativos pode estar correto pelo parser e ainda omitir trabalho
  causal necessário.
- **Próxima verificação:** confirmar criação do ticket do redator/reparo e se o
  helper recebeu sucessor de homologação; manter a mutação aberta até bloco v2
  válido e não tocar produção.

## 15:57 — Gate upstream não governa escritor downstream

- **Observação:** ZCode provou que os strips vivos do worker e redator passam
  as variantes numéricas e que 29 nascimentos estão limpos, mas não encontrou
  a linha que produziu o residual. Atribuiu a causa a patches efêmeros por
  inferência e propôs fail-close no worker, embora uma escrita externa depois
  do worker possa não atravessar esse gate.
- **Decisão:** aceitar apenas testes e inventário; manter produtor como
  `HIPOTESE` e nenhum patch homologado. Acompanhar o complemento do Loop Miguel,
  que exige ordem real, artefatos, teste de fluxo e matriz de caminhos.
- **Resultado:** o sucessor causal agora tem owner ZCode e prazo 16:20; a
  primeira resposta parcial recebeu complemento com matriz: o worker não cobre
  escrita externa, e patches efêmeros permanecem hipótese. O MU plugin sugerido
  é apenas plano e reacopla CONTENT END, metalinguagem e UTM; aguarda revisão.
  O helper e a mutação permanecem problemas separados.
- **Lição:** posicionamento do controle é propriedade do grafo de escrita, não
  só da função testada. Um validador correto não protege caminhos que o
  contornam; mapear caminho×gate×lacuna antes de propor enforcement.
- **Próxima verificação:** auditar a matriz e a evidência do produtor no retorno
  ZCode; exigir ponto inevitável sem regex editorial amplo, sem tocar produção.

## 16:28 — Ponto de controle em revisão não é patch homologável

- **Observação:** o chefe retificou corretamente seu snapshot, mas chamou a
  matriz ZCode de “prova do produtor” e tratou o hook server-side como próximo
  candidato à homologação. Feedback 024 corrigiu: a linha produtora do 265953
  segue ausente, e o MU plugin proposto reacopla CONTENT END, metalinguagem e
  UTM num filtro destrutivo comum.
- **Decisão:** adotar `PONTO_DE_CONTROLE_EM_REVISAO`; não promover arquitetura
  possível a patch candidato. Exigir primeiro invariante mínimo do marcador,
  cobertura de escritores e negativos editoriais.
- **Resultado:** nenhum atraso foi atribuído a ZCode, que respondeu antes de
  16:20; a saída do ticket do índice foi tratada como fechamento sintático.
  Helper e mutação permanecem explicitamente separados e abertos em seus
  estados corretos.
- **Lição:** descoberta do lugar possível de enforcement, desenho mínimo,
  patch testável e homologação são quatro estados. Pular etapas mascara aumento
  de escopo e converte uma hipótese arquitetural em autorização implícita.
- **Próxima verificação:** acompanhar revisão do ponto de controle e exigir
  responsabilidade única/cobertura antes de qualquer candidato, sem produção.

## 17:32 — Cronologia não identifica escritor nem valida apresentação

- **Observação:** ZCode acatou a contenção de fontes inline e não aplicou
  mudança. A resposta encontrou `post_modified` posterior à criação nos três
  posts, mas, sem revisions ou log, chamou de confirmada uma escrita externa
  pós-worker. Também reportou teste 6/6 sem corpus reproduzível e propôs
  converter Markdown em âncora HTML, embora o requisito declarado seja fonte
  invisível.
- **Decisão:** aceitar apenas a contenção; manter causa, ator e patch como não
  homologados. Enviar ao Loop Miguel a diferença entre cronologia e autoria,
  exigir artefato de teste e separar a decisão editorial da transformação.
- **Resultado:** owner ZCode preservado sem ticket duplicado; achado imutável
  novo registra os critérios ainda pendentes. Nenhuma mudança de produção.
- **Lição:** timestamps estabelecem ordem dos eventos, não identidade do ator
  nem caminho de escrita. Preservar informação também não basta: uma
  transformação precisa cumprir separadamente o requisito de apresentação.
- **Próxima verificação:** acompanhar resposta/decisão do Loop Miguel; exigir
  corpus, matriz de escritores e semântica editorial explícita antes de
  qualquer homologação.

## 19:37 — Existência de delta não prova sua semântica

- **Observação:** depois de listar arquivos novos da ronda 039, anunciei antes
  da leitura que o dossiê do patch havia respondido. O bloco canônico mostrou o
  contrário: o prazo 19:15 vencera sem `closes_ref:`; havia apenas nota crítica
  do mentor e novos tickets de outro assunto.
- **Decisão:** corrigir imediatamente a afirmação e mudar o procedimento de
  atualização: antes da leitura, mencionar somente que há delta/arquivos novos;
  resposta, fechamento, atraso e conteúdo só podem ser nomeados depois de ler
  o bloco integral.
- **Resultado:** erro exposto no mesmo ciclo, relatório corrigido pela evidência
  e nenhum estado operacional alterado pela antecipação.
- **Lição:** nome, tamanho, quantidade ou presença de arquivos provam que houve
  mudança, não o que ela significa. Metadado orienta a leitura; não substitui o
  conteúdo.
- **Próxima verificação:** nas próximas rondas, usar linguagem neutra entre a
  listagem do delta e a leitura dos blocos e conferir a afirmação contra o
  `closes_ref:` canônico antes de anunciá-la.

## 20:11 — Append-only não garante identidade única

- **Observação:** a resposta que corrige timestamps futuros foi acrescentada
  duas vezes à mesma fila com o mesmo ID e `closes_ref:`, timestamps distintos
  e um fence Bash aberto no primeiro bloco. O derivado 20:05 declarou zero
  contradições. A resposta ainda atribuiu o dossiê ao commit 19:37:09, mas Git
  prova primeira aparição somente às 19:47:21.
- **Decisão:** não reescrever os blocos; escalar uma correção append-only com ID
  novo e exigir unicidade de ID, fence balanceado, timestamp canônico e
  cronologia Git reproduzida antes de fechar o ticket.
- **Resultado:** o Loop Miguel detectou a colisão no avanço 20:09 e abriu
  sucessor V2 com owner único. Laura evitou duplicar o ticket e encaminhou ao
  mesmo owner apenas a inexatidão Git ainda não coberta, com commits e teste
  reproduzível. Nenhuma mudança de produção foi feita.
- **Lição:** proteção contra mutação de SHA não impede duas adições com a mesma
  identidade. Reserva/inserção precisa rejeitar ID já existente, e o derivador
  deve tratar repetição como contradição crítica, sem escolher silenciosamente.
- **Próxima verificação:** confirmar sucessor/correção com ID único, regeneração
  da saúde e testes negativos para ID repetido, fence aberto e hora Git falsa.

## 20:43 — Delimitador literal é dado e HEREDOC não cotado é expansivo

- **Observação:** a reconciliação da duplicação atribuiu o truncamento às três
  crases do fence. A fila mostra outra condição direta: o exemplo tentou gravar
  uma linha literal `EOF`, igual ao delimitador do HEREDOC; ela e todo conteúdo
  seguinte do primeiro append não aparecem. Crases/dólar em HEREDOC não cotado
  são um agravante de expansão, não a condição de término.
- **Decisão:** complementar o owner corretivo já ativo, sem segunda frente, e
  separar causa primária de agravantes. Preferir ferramenta de patch/escrita sem
  shell para Markdown; se HEREDOC for inevitável, delimitador único e cotado,
  ausência provada no corpo e validação pós-write.
- **Resultado:** causa reproduzida por linhas do arquivo e anexada ao mesmo
  incidente. A reconciliação também falhou no `closes_ref:` ao substituir setas
  por hífens; o Loop Miguel detectou e abriu correção antes do prazo.
- **Lição:** um delimitador escolhido pelo programa também pode existir nos
  dados. Separar código de conteúdo evita que texto do relatório controle o
  shell; comparação de identidade precisa ser byte a byte.
- **Próxima verificação:** confirmar resposta com ref exata, causa corrigida,
  MUT reconciliado e índice/saúde regenerados, sem reescrever histórico.

## 21:14 — Hash não é rollback e contenção não cria owner causal

- **Observação:** o 265985 nasceu às 20:23 com o mesmo `CONTENT END 1` do
  265953. O strip client-side evitou exposição pública, mas guardou apenas hash
  truncado; não existe snapshot completo ou revision restaurável. A resposta
  atual atribui a causa a regex sem variante numérica no redator/reparo, em
  tensão com testes anteriores que diziam cobrir variantes sem achar produtor.
- **Decisão:** não duplicar o reparo do post. Escalar somente a reincidência
  causal, pedindo owner separado, captura de fonte/worker/redator/pre-save no
  próximo nascimento e snapshot completo antes de qualquer strip.
- **Resultado:** risco imediato contido e agendamento preservado; lacuna de
  rollback reconhecida pelo Loop Miguel; achado causal novo enviado sem aplicar
  patch nem alterar produção.
- **Lição:** hash verifica identidade, mas não reconstrói bytes. Uma contenção
  destrutiva precisa preservar a pre-image antes de remover a evidência; fechar
  o post não fecha o produtor recorrente.
- **Próxima verificação:** confirmar sucessor causal com owner/prazo e captura
  por estágio; acompanhar três eventos documentais separados do MUT/265985.

## 21:46 — Saúde vazia não prova causa resolvida

- **Observação:** três eventos separados reconciliaram MUT 915a e fecharam os
  tickets documentais; o mantenedor passou a aceitar o ID público com hashes e
  fechamento exatos. Em paralelo, ZCode apontou regravação sem strip em
  `generate_upload_attach_cartoon`, mas sem SHA/diff/trace do 265985. O índice
  ficou sem ativos embora o alerta causal ainda não tenha owner.
- **Decisão:** aceitar o ciclo de vida do MUT como encerrado e o patch do
  mantenedor por inspeção/execução real, declarando que a suíte local não rodou
  por ausência de Python. Manter a função do worker como causa candidata e não
  abrir novo alerta além do existente.
- **Resultado:** MUT 915a aparece reconciliado, c991 permanece aberto; causa
  CONTENT END segue pendente de sucessor e artefatos, sem patch/homologação.
- **Lição:** zero ativos é propriedade do modelo documental, não prova de
  inexistência de trabalho causal. E “teste indisponível” precisa ser distinto
  de “teste verde”; inspeção estática e execução operacional têm alcances
  diferentes.
- **Próxima verificação:** confirmar owner causal e obter arquivo/SHA/diff/trace
  da regravação; executar a suíte quando houver runtime já autorizado/disponível.

## 22:17 — Objeto de arquivo não é caminho literal

- **Observação:** usei `Get-Content $_` numa pipeline de `FileInfo`; o binding
  tratou somente o nome como caminho relativo e produziu falsos arquivos
  ausentes nas caixas obrigatórias.
- **Decisão:** não inferir caixa vazia; repetir com `-LiteralPath $_.FullName`.
- **Resultado:** todas as caixas foram lidas (`mandatory_mailboxes=OK`), sem
  perda de mensagem ou efeito externo.
- **Lição:** em leitura iterada, passar o caminho absoluto explicitamente;
  erro de resolução é falha do comando, não evidência sobre o conteúdo.
- **Próxima verificação:** manter `FullName` nas próximas rondas e registrar
  separadamente erro de ferramenta e estado da fonte.

## 22:52 — Silêncio de stdout não é exit code

- **Observação:** usei `if (git diff --quiet ...)` no PowerShell para decidir
  se três filas mudaram. O processo externo não emite saída; testar essa saída
  como booleano imprimiu `SIM` embora o delta nominal não listasse as filas.
- **Decisão:** descartar o rótulo auxiliar e repetir cada comando, lendo
  `$LASTEXITCODE`: `0` sem diferença, `1` com diferença, outro valor erro.
- **Resultado:** as três filas foram confirmadas inalteradas (`0`) e nenhum
  estado ou alerta foi inferido da saída falsa.
- **Lição:** comandos externos comunicam sucesso semântico pelo contrato de
  exit codes, não pela presença/ausência de stdout; wrappers precisam validar
  explicitamente ambos quando a classificação depende disso.
- **Próxima verificação:** em todo `git diff --quiet`, testar `$LASTEXITCODE`
  imediatamente e falhar fechado para códigos fora de `0/1`.

## 23:24 — Horário observado não é estimativa

- **Observação:** a ronda 045 declarou `22:52:50`, mas seu primeiro commit foi
  às 22:51:00; eu havia estimado um término futuro, apesar da lição já vigente.
  Depois, o wait da ronda 046 recebeu timeout fixo de 1.800 s quando faltavam
  1.881 s até o alvo e encerrou 81 s cedo.
- **Decisão:** preservar o relatório imutável com limite seguro
  `antes_de_22:51:00`; capturar o relógio por comando concluído antes de cada
  escrita. Para waits, derivar timeout do alvo absoluto mais margem e, se
  necessário, rearmar somente o restante.
- **Resultado:** Claude aplicou a régua correta; a ronda 046 foi rearmada e
  começou exatamente às 23:22:50, sem antecipação ou perda de cadência.
- **Lição:** horário previsto não é evidência temporal. Tanto metadado quanto
  timeout precisam derivar do relógio observado e do alvo absoluto.
- **Próxima verificação:** comparar timestamp declarado com commit após o push
  e rejeitar qualquer valor posterior à primeira evidência Git.
