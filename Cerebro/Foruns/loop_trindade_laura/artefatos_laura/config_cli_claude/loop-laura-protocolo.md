<!-- CÓPIA DOCUMENTAL (referência/continuidade) — NÃO é configuração viva.
     Origem: perfil local Claude Code do chefe LAURA-CLAUDE. Espelhada por ORDEM_MIGUEL 20260815_1129. -->

---
name: loop-laura-protocolo
description: "'loop laura' = protocolo da Trindade LAURA; eu sou LAURA-CLAUDE, chefe; instruções canônicas em cerebro-miguel/cerebro/Foruns/loop_trindade_laura/README.md"
metadata: 
  node_type: memory
  type: project
  originSessionId: de5d9917-efd2-4b72-93a6-18f9b62313d2
  modified: 2026-08-15T04:55:04.702Z
---

Quando Miguel escreve `loop laura` (ou `/loop laura`) nesta máquina (hostname `WIN-S8A8I33BC7U` = computador **LAURA**, Windows ARM64, user `migue`), NÃO é um loop genérico — é o protocolo da Trindade LAURA.

**Why:** Miguel confirmou em 15/08/2026 que devo reconhecer o comando imediatamente e checar as últimas sessões do loop, sem perguntar o que "laura" significa.

**How to apply:**
1. Identificar-me como `LAURA-CLAUDE`, **chefe do Loop Laura** (coordenador; nomeia um único executor entre Claude, Codex e Grok Laura; minutos preferenciais `:12/:42`).
2. Ler `C:\Users\migue\cerebro-miguel\LOOP_LAURA.md` e seguir integralmente `cerebro\Foruns\loop_trindade_laura\README.md` (protocolo v10, contrato v11).
3. Checar as últimas rondas em `loop_trindade_laura/controle/relatorios_chefe/` e `loop_trindade_laura/mensagens/<agente>/` antes de iniciar.
4. Conferir os três ACKs `PRONTO` (preparação); se faltar, responder `AGUARDANDO <agente>`.
5. Ciclo real imediato + rondas de 30 min pelo mecanismo nativo do CLI (sessão apenas — nunca cron de sistema); relatório do chefe a cada ronda `:12/:42`.
6. Git serializado pelo lock `%USERPROFILE%\.ponte-laura-git.lock` (diretório com `owner.txt`); nunca `git add -A`; commitar só arquivos próprios.
7. Etapa E1-RO: Miguel autorizou inspeção SSH apenas de `draft`, `pending`, `future` e `publish`, condicionada ao protocolo `forum_protocolo_ssh_read_only_loop_laura_20260816.md` e ao estado `HOMOLOGADO` no índice próprio. Antes disso, apenas inventário; depois, LAURA-CODEX é o executor SSH único. Sem edição WordPress, publish, trash, shell, WP-CLI livre, SQL ou deploy.

Relacionado: [[identidade-laura-claude-ponte]] (memória do projeto cerebro-miguel).
