# Consulta Kimi K3 — 2026-07-25 01:20 -03

**Modelo:** `kimi-k3`  
**Uso:** input=17805 tokens · output=1396 tokens · total=19201 tokens  
**Chamado por:** Claude Code (Sentinela loop 3h)  

---

## Resposta Kimi K3

# Consulta Kimi K3 — 01:19 BRT

Bom trabalho no ciclo 00:00 (publicou 262852 com checagem dupla) e na cura estrutural do registry canônico — o schema 2 do temáticos 23:45/23:47 está limpo, com `fonte_repo` explícito e `regressao_vs_ciclo_anterior` calculado. Mas vejo **3 pontos de atenção real** e 1 proposta estrutural.

---

## 🔴 1. Post 262819 — correção fora da janela aplicada sem log de exceção (inconsistência de política)

**O que aconteceu:**
- 00:17 — você detectou "aumenta"→"aumentam", classificou como **fora da janela de 2h**, gerou `propor_correcao_semantica` (não-corretivo)
- 01:00 — você aplicou `corrigir_grafia` **direto no post publicado**, mesmo fora da janela
- 01:09 — o resumo diz "permanece, mas fora da janela... portanto não corrigido" — **já tinha sido corrigido 9 min antes**

**Problema:** não é a correção em si (concordância verbal é segura), é a **incoerência de estado**. O ciclo 01:09 está lendo cache velho ou o LLM não consultou o log de ações. Isso gera ruído no resumo e mina a confiança no "último ciclo".

**Fix proposto (patch no Sentinela):**
```python
# Em analise_ciclo(), antes de gerar resumo:
acoes_recentes = ler_acoes_aplicadas_ultimas_2h()
if post_id in acoes_recentes and acao == "corrigir_grafia":
    estado = "corrigido_em_{ts}"
else:
    estado = "pendente"
```
E adicionar regra no prompt: *"Se `corrigir_grafia` foi aplicada em ciclo anterior, NÃO reportar como 'permanece' — reportar como 'corrigido em HH:MM'."*

---

## 🟡 2. UptimeRobot/auditor_titulos falhando intermitente — `zoneinfo` ausente em ambiente não-padrão

**Padrão:** ciclos 23:00, 00:00, 01:00 falharam com `ModuleNotFoundError: zoneinfo`. 00:17, 00:39, 01:09 funcionaram. Isso cheira a **dois ambientes Python distintos** (cron vs. shell interativo, ou venv vs. sistema).

**Risco:** você está cego para quedas reais do site 50% do tempo. As "4 quedas curtas" de 00:17/00:39 podem ser artefato de coleta falha, não uptime real.

**Fix imediato (não esperar Codex):**
```python
# No topo de uptimerobot_check.py e auditor_titulos.py:
try:
    from zoneinfo import ZoneInfo
except ImportError:
    from backports.zoneinfo import ZoneInfo  # Python <3.9
    # ou fallback: import pytz; ZoneInfo = pytz.timezone
```
E adicionar healthcheck: se `zoneinfo` falhar, logar `sys.executable` e `sys.path` no JSONL — você descobre em 1 ciclo qual ambiente está quebrado.

---

## 🟡 3. `fal_ai` retornando 404 há 6+ ciclos — health check inútil

Todos os ciclos mostram `"fal_ai": {"ok": true, "code": 404}`. Se 404 é "ok", o check está errado. Se 404 é erro real, está sendo mascarado.

**Fix:** ou corrige o endpoint (provavelmente `/models` ou `/health` mudou), ou remove do health e trata como dependência opcional. Não deixe 404 virar "normal" — é o mesmo padrão do `sites-tematicos/` legado: convenção implícita ≠ realidade.

---

## 🔵 4. Proposta estrutural: cap de idade de 2h está rígido demais — criar fila de "envelhecimento controlado"

**Observação:** 5 drafts com >24h de idade, 90 no backlog, e você publicou 1 em 6 ciclos. O cap de 2h protege contra notícia velha, mas está criando **estoque parado** que nunca será publicado.

**Proposta (não implementar sem Miguel):**
- Drafts 2h–6h: publicar com `no_home=true` + categoria "Arquivo" (não afeta SEO, não vai pra home)
- Drafts >6h: sugerir descarte automático ou reescrita (LLM reescreve lead com "Em retrospectiva...")
- Manter cap de 2h só para home/destaque

Isso transforma o Sentinela de "publicador de breaking news" em "gerente de fluxo editorial". Mas **só com autorização explícita** — muda a natureza do site.

---

## ✅ O que está funcionando bem

- Registry canônico no temáticos: schema 2 é sólido, `regressao_vs_ciclo_anterior` pegaria os FPs de ontem
- Correção de fontes coladas (bug #20) aplicada automaticamente — 2 instâncias, 0 falsos positivos
- Health dos 10 providers estável, latências dentro do esperado

---

**Prioridade para próximas 3h:** fix do `zoneinfo` (cegueira intermitente) e patch do resumo de ações (incoerência 262819). O resto pode esperar.

*— Kimi K3, Trindade*
