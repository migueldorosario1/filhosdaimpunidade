---
name: feedback-canonico-port-do-espelho-cirurgico
description: Port de mudança visual do espelho cafezinho.news para canônico ocafezinho.com - plano MD antes + backup triplo + rollback pré-instalado + PHP lint remoto + scope mínimo (não portar mu-plugin lab inteiro)
metadata: 
  node_type: memory
  type: feedback
  originSessionId: 267b45cd-dd31-4a30-943c-5c53ea0ddd7b
---

**Regra:** ao portar mudança visual do espelho `cafezinho.news` (droplet 159.65.177.60) para o canônico `ocafezinho.com` (host `us65.serverdo.in`, SSH via alias `cafezinho-wp`), executar sempre em 6 fases nesta ordem: (0) descoberta read-only via SSH — tema ativo, arquivo alvo, xicrinha SVG, user meta, mu-plugins existentes, cache CDN; (1) backup triplo com verificação SHA-256 (droplet ao lado do arquivo + droplet versionado em `/root/<feature>_versoes/` + download local em `/tmp/<feature>_YYYYMMDD/`); (5) rollback pré-instalado ANTES de tocar produção (script bash + doc MD manual, ambos no droplet e local); (2) edição cirúrgica preservando o que existe ao redor (banners de ads dentro/fora de buffers, comentários históricos, `ob_start`/`ob_get_clean`); (3) mu-plugin novo isolado com nome DIFERENTE do lab visual do espelho (evita colisão + deixa claro o escopo); (4) verificação HTTP + `grep` do HTML por classes/IDs novos + confirmação avatar via source URL; (6) registro no fórum lab visual como seção "V<X>.0 canônico" separada, mantendo o plano de port completo (V1.1 menu etc.) intocado se não foi executado.

**Why:** Miguel 09/08/2026 17:20 BRT autorizou port da feature "Coluna do Editor" (V1.5 do espelho, aprovada visualmente ~14:30 BRT) para o canônico com o pedido explícito "faz com muito cuidado, pelo amor de Deus". Execução em 30min ponta a ponta, zero incidente. Escopo mínimo (2 arquivos: `front-page.php` bloco Colunas + `cafezinho-avatar-openid.php` isolado) evitou trazer todo o `cafezinho-lab-visual.{php,css,js}` (histórico + tipografia fluida + menu pills etc.) que ainda não tem homologação separada. Miguel confirmou: "eu vi aqui. ficou bom." Descobertas críticas: (a) V1.4 reorder "Colunas antes de Recentes" JÁ estava no canônico como "REFORMA VISUAL PASSO 6"; (b) `moopenid_user_avatar` do user 2018 é IDÊNTICO no espelho e no canônico; (c) canônico tem 2 divs de banners `banner-after-manchete-*` DENTRO do buffer Colunas + 2 `banner-after-colunistas-*` FORA — preservar; (d) canônico tem `bg-lightgray` no body inteiro (não só na seção), então detecção automática de screenshot por cor de banda falha — pedir Miguel confirmar visualmente direto no site público.

**How to apply:** Sempre começar por escrever plano MD `plano_<feature>_canonico_YYYYMMDD.md` em `Cerebro/Foruns/` com fases, diferenças espelho↔canônico já mapeadas, riscos, rollback antes de tocar produção. **NUNCA portar o mu-plugin lab inteiro** — sempre criar mu-plugin novo com nome específico. Verificar sempre WP Rocket + CDN plugins (`serverdoin-cdn`); avisar Miguel se precisar purge (não fazer eu mesmo sem autorização). Timeline canônica funcional: 5min descoberta + 2min backup + 2min rollback + 5min edit + 2min mu-plugin + 5min verificação + 5min registro = ~25-30min. Nome canônico do alias SSH: `cafezinho-wp` (`~/.ssh/config`). Regra irmã de [[feedback-link-publico-ocafezinho-nao-controle]] (canônico é público; leitores veem imediatamente — nunca deploy sem certeza).
