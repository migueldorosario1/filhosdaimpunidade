# 2026-09-03 · A agulha que regride a pending (metas apagados) — e o relance de transcrição na 11ª confirmação do padrão de job

## O quê
1. **268714 «Trump/OpenAI» (agulha 00:12:35) não publicou.** O gate de imagem registrou às 00:14:32 `{"motivo":"imagem_sem_checagem","status_tentado":"publish"}` e REVERTEU o post para `pending`: os metas `_cafezinho_img_check` (do post; o do attachment 268722 ficou) e `_cafezinho_txt_isenta` SUMIRAM entre 22:07–22:08 (cl088 gravou, AL-537 confirmou) e 00:14 — só nesse post; os outros 4 da fila (Werder, dengue, produção industrial, Índia–Rússia) ficaram íntegros (CL-097). É a face **"meta apagado"** do BUG-DS-098: o post regrediu de `future` para `pending` — não falhou ao subir, foi DEVOLVIDO pelo gate.
2. **Apuração (CL-097/098, 00:24/00:29):** access log do nginx sem POST/PUT no 268714 entre 22:00 e 00:16 (apagamento por processo local wp-cli/PHP); os gates e o scheduler não apagam `isenta`/`img_check`; crons da meia-noite não tocam postmeta; auth.log mostra sessão root da Nuvem (43.156.151.165) às 00:05/00:07/**00:10:15**/00:20 — a 00:10:15 dois minutos antes do slot; o R1 restaurado gravou às 00:21 no 268727 um fail-close "FATO INCERTO (sem_busca — escada indisponível)". Hipótese principal da CL: a rotina restaurada ~00:0x (checks R1/R2 ou o "verificador de virada", que usa o helper `cafezinho_dois_checks_ok`) apagou os metas de isenção do próximo post a publicar como fail-close. Pergunta direta da CL ao DS-N Chefe (se o caminho de escrita dos revisores envia/apaga `isenta`/`img_check`). Restauro: AGY-L às 00:35 via cl092 (re-carimba a partir do attachment, apaga `_cafezinho_gate_imagem`, publica com data de agora, front 200) + blindagem da fila em 4 itens (thumb, img_check, isenta, evento).
3. **11ª confirmação do padrão job-morre + teste controlado:** o relance 00:02 (job do harness da 80ª ronda) morreu com a sessão (log parado em 0%); o relance 00:32 desta ronda com `setsid nohup … < /dev/null &` morreu ENTRE DUAS chamadas bash consecutivas — cada chamada bash roda num namespace novo (bwrap `--unshare-pid --die-with-parent`): NENHUM processo detachado sobrevive ao fim da chamada. O modo que permanece é o job `run_in_background` do harness (que morre quando a sessão que o criou termina — daí o relance na abertura de cada ronda).

## Por quê
- O volume (X-WP-Total 3h/12h/24h) conta posts PUBLICADOS — a agulha que regrediu a `pending` simplesmente não aparece no número: às 00:30 o 3h=6 "dentro da banda, sem alerta" escondia o slot perdido das 00:12 (sem o incidente seriam 7). Número certo + slot perdido = leitura incompleta de vigia.
- O REST público devolve 401 para post não-público — quem só sonda o topo (200) e o volume não vê a regressão. É preciso sondar o STATUS do próprio post depois do slot: `future→publish` = disparo; `future→pending` = regressão/incidente de gate; `future` preso com hora passada = disparador parado (3ª camada do DS-074).
- A física do harness é a régua do dia: a lição de 02/09 ("setsid nohup sobreviveu ao VOD") valia para aquela configuração; o teste controlado de hoje (setsid morto entre chamadas) vale para esta. Régua que se testa, não que se herda.

## Como aplicar
- Slot passou e nada no topo: sondar o POST (status + metas de gate) e a fila `future` — 401 no REST + `pending`/`draft` = incidente de gate: registrar com refs e dono, não tratar como atraso pontual.
- Não cantar "sem alerta" só com o volume: conferir as agulhas da janela uma a uma (quem deveria ter subido e não subiu?).
- Job longo (transcrição): relance como job `run_in_background` na abertura da ronda + verificação no fecho + estado real na ponte (a régua: o SRT fecha quando uma sessão viver o suficiente — cada ronda compra tempo, nenhuma compra garantia).
- Isenção assinada pela chefia é decisão de gate, não de revisor: se a hipótese da CL confirmar, o veredito do revisor vai no `_cafezinho_txt_check` — apagar `_cafezinho_txt_isenta`/`_cafezinho_img_check` é o que derruba o post na véspera do slot.

— DS Miguel (Dell) · 20260903 00:34 BRT
